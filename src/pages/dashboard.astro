---
import Layout from '../layouts/Layout.astro';

// We keep the entries list here so the left sidebar keeps working!
const entries = [
  { id: 1, title: "Settlers of Catan", thumb: "/colonist thumb.png", link: "/entry-one" },
  { id: 2, title: "Balatro", thumb: "/balatro square.avif", link: "/entry-two" },
  { id: 3, title: "9 Kings", thumb: "/9 kings thumb 1.jpg", link: "/entry-three" },
  { id: 4, title: "Final Thoughts", thumb: "https://placehold.co/300x450/2a475e/c7d5e0?text=Final+Thoughts", link: "/entry-four" },
];
---

<Layout title="LIBRARY">
  <header class="steam-ribbon">
    <div class="nav-arrows">
      <a href="/">&larr;</a>
      <a href="#" class="disabled">&rarr;</a>
    </div>
    <nav class="main-nav">
      <a href="/upcoming">UPCOMING</a>
      <a href="/dashboard" class="active">PLAYED</a>
      <a href="/what">WHAT ARE WE DOING HERE AGAIN?</a>
    </nav>
  </header>

  <div class="app-layout">
    
    <aside class="sidebar">
      <div class="search-bar">
        <input type="text" placeholder="Search..." />
      </div>
      <ul class="game-list">
        {entries.map((entry) => (
          <li><a href={entry.link}>{entry.title}</a></li>
        ))}
      </ul>
    </aside>

    <main class="library-grid">
      <h2>Recent Entries</h2>
      <div class="thumbnails">
         {entries.map((entry) => (
          <a href={entry.link} class="game-card">
             <img src={entry.thumb} alt={entry.title} />
          </a>
        ))}
      </div>
    </main>
    
  </div>
</Layout>

<style>
  /* Ribbon Styling */
  .steam-ribbon {
    background-color: var(--steam-header-bg);
    padding: 1rem 2rem;
    display: flex;
    align-items: center;
    gap: 2rem;
    border-bottom: 1px solid #2a475e;
  }

  .nav-arrows a {
    color: var(--steam-text);
    font-size: 1.5rem;
    margin-right: 0.5rem;
  }
  .nav-arrows .disabled { color: #3a4048; cursor: not-allowed; }

  .main-nav {
    display: flex;
    gap: 2rem;
    font-size: 1.1rem;
    font-weight: 500;
  }

  .main-nav a {
    color: var(--steam-text);
    padding-bottom: 0.5rem;
    transition: color 0.2s;
  }

  .main-nav a:hover { color: white; }

  /* The active tab with blue text and underline */
  .main-nav a.active {
    color: var(--steam-blue);
    border-bottom: 3px solid var(--steam-blue);
  }

  /* Two-Column App Layout */
  .app-layout {
    display: flex;
    height: calc(100vh - 70px);
  }

  /* Sidebar Styling */
  .sidebar {
    width: 300px;
    background-color: #2a2d33;
    padding: 1rem;
    overflow-y: auto;
  }

  .search-bar input {
    width: 100%;
    padding: 0.5rem;
    background: #171a21;
    border: none;
    color: white;
    border-radius: 4px;
    margin-bottom: 1rem;
  }

  .game-list { list-style: none; }
  
  .game-list li a {
    display: block;
    padding: 0.25rem 0.5rem;
    font-size: 0.9rem;
  }
  .game-list li a:hover {
    background: #3a4048;
    color: white;
  }

  /* Main Grid Window Styling */
  .library-grid {
    flex: 1;
    padding: 2rem;
    background: linear-gradient(to bottom, #1b2838 0%, #171a21 100%);
    overflow-y: auto;
  }

  h2 {
    color: white;
    margin-bottom: 1rem;
    font-weight: 400;
  }

  .thumbnails {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .game-card {
    display: block;
    width: 200px;
    transition: transform 0.2s, outline 0.2s;
  }

  .game-card img {
    width: 100%;
    border-radius: 4px;
    aspect-ratio: 2 / 3;   /* Forces the tall rectangle shape */
    object-fit: cover;     /* Crops perfectly without squishing */
  }

  .game-card:hover {
    transform: scale(1.02);
    outline: 2px solid white;
  }
</style>